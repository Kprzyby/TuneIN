import styled from "styled-components";

export const RemoteCamera = styled.video`
    display: block;
    aspect-ratio: 16 / 9;
`;

export const CameraWrapper = styled.div`
    display: block;
    background: darkgrey;
    width: 350px;
    height: auto;
`;